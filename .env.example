# PagoKit — Stripe integration (test mode only, Rule 8)
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
PUBLIC_URL=http://localhost:3000

# Shared secret for POST /api/refund (no user accounts in this app, so this
# is the only access control on refunds — keep it out of git, rotate if leaked)
ADMIN_REFUND_SECRET=REPLACE_ME
